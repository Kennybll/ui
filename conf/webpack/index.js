'use strict';

const dev = require('./Dev');
const dist = require('./Dist');
const test = require('./Test');
const server = require('./Server');

module.exports = {
  dev,
  dist,
  test,
  server
};
